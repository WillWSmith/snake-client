// IP address of the game server
const IP = "localhost";

// Port number for the game server
const PORT = "50541";

// Player's name sent to the server
const NAME = "Name: WWS";

// Commands for player movement
const DOWN = "Move: down";
const UP = "Move: up";
const LEFT = "Move: left";
const RIGHT = "Move: right";

// Messages for communication in the game
const B = "Say: Boo!";
const V = "Say: LOL!";
const C = "Say: Come get me!";

// Exporting all constants for use in other modules
module.exports = {
  IP,
  PORT,
  NAME,
  DOWN,
  UP,
  LEFT,
  RIGHT,
  B,
  V,
  C,
};
